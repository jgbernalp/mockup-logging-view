import * as React from 'react';
import { PageSection, Title } from '@patternfly/react-core';

const Dashboard: React.FunctionComponent = () => (
  <PageSection>
    <Title headingLevel="h1" size="lg">
      Welcome to the Logs view mockup 📖!
    </Title>
  </PageSection>
);

export { Dashboard };
